export default function DbConnectionLayout({
                                               children,
                                           }) {
    return (<section>
        <h1>Database Connection</h1>
        {children}
    </section>)
}