"use client"
import React from "react";

export default function DataExplorerLayout({
                                            children,
                                        }: {
    children: React.ReactNode;
}) {
    return (<>
        {children}
    </>)
}